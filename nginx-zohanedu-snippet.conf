# Paste inside your site's `server { }` for zohanedu.in (aaPanel: Website → Conf)
# Root must be: /www/wwwroot/zohanedu

root /www/wwwroot/zohanedu;
index index.html;

location / {
    try_files $uri $uri/ $uri.html =404;
}
